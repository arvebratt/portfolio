import { Container, Grid, Paper } from '@material-ui/core';

const Contact = () => {
    return (
        <Container fixed>
            <Grid container></Grid>
        </Container>
    );
};

export default Contact;
